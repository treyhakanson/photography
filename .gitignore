node_modules
dist

# Full-resolution originals. media/ is derived from these and committed;
# see "Downsizing" in the README.
raw_media/

# Per-machine record of what produced media/; source mtimes make it churn.
.derived.json

.DS_Store
*.local
